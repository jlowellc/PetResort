@model IEnumerable<ThePetResort.DAL.ReservationLimit>

@{
    ViewBag.Title = "Index";
}

<div class="row">
    <div class="col-lg-12">
        <ol class="breadcrumb">
            <li>@Html.ActionLink("Home", "Index", "Home")</li>
            <li class="active">Reservation Limits</li>
        </ol>
    </div>
</div>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Limit)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ResortLocation.ResortName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Type.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Limit)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ResortLocation.ResortName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Type.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ReservationLimitID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ReservationLimitID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ReservationLimitID })
        </td>
    </tr>
}

</table>
